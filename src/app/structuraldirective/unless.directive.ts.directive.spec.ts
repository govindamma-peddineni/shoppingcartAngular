import { Unless.Directive.TsDirective } from './unless.directive.ts.directive';

describe('Unless.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new Unless.Directive.TsDirective();
    expect(directive).toBeTruthy();
  });
});
