import { RecipesListDirective } from './recipes-list.directive';

describe('RecipesListDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipesListDirective();
    expect(directive).toBeTruthy();
  });
});
