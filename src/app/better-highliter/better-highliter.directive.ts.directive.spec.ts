import { BetterHighliter.Directive.TsDirective } from './better-highliter.directive.ts.directive';

describe('BetterHighliter.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHighliter.Directive.TsDirective();
    expect(directive).toBeTruthy();
  });
});
